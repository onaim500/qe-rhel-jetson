# Jetson samples image (runtime + sample streams/models).
# Override with DEEPSTREAM_IMAGE for Triton or deepstream-l4t tags.
ARG DEEPSTREAM_IMAGE=nvcr.io/nvidia/deepstream:7.1-samples-multiarch
FROM ${DEEPSTREAM_IMAGE}
WORKDIR /opt/nvidia/deepstream/deepstream
